# simple-postgresql-high-avaliability

## Обзор

SPGHA представляет собой набор bash скриптов для поддержания репликационной пары PostgreSQL в рабочем состоянии.
Вместе со скриптами предполагается использование Keepalived для предоставления доступа к мастер-инстансу Postgres по VIP.

## Установка

SPGHA поставляется в качестве набора bash скриптов и предполагает ручную установку. Также скрипт опирается на Keepalived для предоставления единой точки входа в кластер, примеры конфигурации 

### Подготока системы

1. **Добавление директорий для работы скрипта**

    На всез узлах SPGHA в рабочей директории скрипта должна присутстваовать директория для системы сигналов и блокировок. На узлах-контроллерах также можно добавить директорию для архивирования логов СУБД.
    В рабочие директории разместите скрипты `pgha-controller.sh` или `pgha-voter.sh` в зависимости от роли узла.
    Для рабочей директории и ее содержимого установите владельца **root:root** с правами **700**.
    Пример структуры раобочего директорий скрипта:

    ```txt
    На контроллере:
    spgha
    ├── pgha-controller.sh
    │
    ├── locks
    │   └── ...
    │   
    └── [pg_log_archive]
        └── ...
    ```

    ```txt
    На голосующем узле:
    spgha
    ├── pgha-voter.sh
    │
    └── locks
        └── ...
    ```

1. **Добавление systemd сервисов**

    Запуск скриптов осуществляется через systemd по

    На узлах с **полновесным** контроллером spgha добавьте systemd сервис:
    `vim /lib/systemd/system/spgha_active_node_controller.service`

    ```conf
    [Unit]
    Description=Manages high avaliability for PostgreSQL replication pair
    Wants=network-online.target
    After=network-online.target
    StartLimitIntervalSec=3600
    StartLimitBurst=2

    [Service]
    Type=simple
    ExecStart=<путь до скрипта spgha>
    KillMode=process
    TimeoutStopSec=5
    Restart=on-failure
    RestartSec=5

    [Install]
    WantedBy=multi-user.target
    ```

    На узлах с **голосующим** контроллером spgha добавьте systemd сервис:
    `vim /lib/systemd/system/spgha_voter_node_controller.service`

    ```conf
    [Unit]
    Description=A lightweight voter for Simple PostgreSQL High Avaliability
    Wants=network-online.target
    After=network-online.target
    StartLimitIntervalSec=3600
    StartLimitBurst=2

    [Service]
    Type=simple
    ExecStart=<путь до скрипта spgha>
    KillMode=process
    TimeoutStopSec=5
    Restart=on-failure
    RestartSec=5

    [Install]
    WantedBy=multi-user.target
    ```

После добавления новых сервисов перечитайте конфигурацию systemd
`systemctl daemon-reload`

### Подготовка keepalived

В процессе работы SPGHA опирается на keepalived для предотвращения подключения к СУБД в сценарии **split brain** и предоставления единой точки входа.
Поэтому одним из этапов подготовки является установка и конфигурация сервиса keepalived.

В данной установке механизм VRRP не применяется, а узел, на котором необходимо поднять VIP, определяется следующим скриптом:

```bash
#!/bin/bash

PG_ISREADY="<Путь до утилиты pg_isready>"
PSQL="<Путь до утилиты psql>"

if [[ -e /etc/keepalived/no_vip.block ]]; then
    exit 1
fi

if ! "$PG_ISREADY" -q -h 127.0.0.1 -p 5432 -U postgres; then
    exit 1
fi

is_slave=$("$PSQL" -h 127.0.0.1 -p 5432 -U postgres -tAc "SELECT pg_is_in_recovery();")
if [[ "$is_slave" = "t" ]]; then
    exit 1
else
    exit 0
fi
```

### Подготовка репликационной пары PostgreSQL

### Конфигурация скриптов для работы в двухузловом режиме

### Конфигурация скриптов для работы с голосующим узлом

## Сценарии работы

### Репликационная пара PostgreSQL sync-only

Вариант работы скрипта, на 2-х узнал параллельно PostgreSQL в режиме высокой надежности.

### Репликационная пара PostgreSQL standalo

Вариант работы скрипта, на 2-х узнал параллельно PostgreSQL.

### Репликационная пара PostgreSQL + голосующий узел

## Параметры конифгурации

> Все параметры должны быть указаны перед запуском скрипта

### Конфигурационные параметры активного узла

| Параметр | Описание |
| --- | --- |
| **Фийлы и директории** | |
| Директории скрипта | |
| LOGFILE_PATH | Абсолютный путь до файла в который будут писаться логи скрипта. <br> Файл может отсутствовать, но директория файла должа существовать. |
| LOCK_FILE_DIR | Абсолютный путь до служебной директории скрипта, предназначенной для системы сигналов и блокировок. <br> Должна существовать до запуска скрипта. <br> Должнен быть идентичен на всех узлах со скриптом. |
| KEEPALIVED_DIR | Абсолютный путь до директории с конфигурацией keepalived и скриптом-определителем роли локального инстанса СУБД. <br> Должна существовать до запуска скрипта. |
| PG_LOG_ARCHIVE_DIR | Абсолютный путь до директории, в которую будут архивироваться логи СУБД, перед деструктивными действиями. <br> Должна существовать до запуска скрипта, если планируется использовать функционал архивирования. |
| Директории PostgreSQL | |
| PGDATA | Абсолютный путь до директории данных Postgres. |
| PGWAL | Абсолютный путь до директории журнала предзаписи Postgres. |
| PGLOG | Абсолютный путь директории, содержащей текстовые логи Postgres. |
| Бинарники PostgreSQL | |
| PG_ISREADY | Абсолютный путь до утилиты pg_isready. |
| PG_CTL | Абсолютный путь до утилиты pg_ctl. |
| PSQL | Абсолютный путь до утилиты psql. |
| PG_CONTROLDATA | Абсолютный путь до утилиты pg_controldata. |
| PG_BASEBACKUP | Абсолютный путь до утилиты pg_basebackup. |
| PG_REWIND | Абсолютный путь до утилиты pg_rewind. |
| **Пользователи** | |
| SYSTEM_ADMIN_USER | Системный пользователь - администратор Linus. <br> Предполагается root. |
| SYSTEM_POSTGRES_USER | Системный пользователь - администратор postgres. <br> Предполагается postgres. |
| PG_ADMIN_USER | Пользователь СУБД с правами суперпользователя. Используется для проверки состояния СУБД и для утилиты pg_rewind. <br> Предполагается postgres. |
| PG_REPLICATION_USER | Пользователь СУБД с правами репликации. Используется для настройки репликации между узлами и для снятия базовых РК утилитой pg_basebackup. |
| **Информация о кластере** | |
| CLUSTER_NAME_LOCAL | Имя локального кластера. Используется для записи в логи и для идентификации синхронной реплики. <br> Устанавливается произвольно и должно быть зеркально в конфигурациях активных узлов. |
| CLUSTER_NAME_REMOTE | Имя удаленного кластера. Используется для записи в логи и для идентификации синхронной реплики. <br> Устанавливается произвольно и должно быть зеркально в конфигурациях активных узлов. |
| CLUSTER_IP_REMOTE | IPv4 адрес удаленного узла. |
| CLUSTER_IP_VOTER | IPv4 адрес голосующего узла. |
| PGPORT | Порт на котором принимает подключения Postgres. |
| **Параметры ретраев и таймаутов** | |
| SSH_CONNECT_TIMEOUT | Выставляет параметр **ConnectTimeout** для SSH соединений, используемых скриптом. Определяет время в секундах которое выделяется утилите SSH для установления соединения. <br> Рекомендованное значение **1** для быстроко обнаружения отказа сети. <br> *Реализовано в целях разработки и не рекомендуется к изменению.* |
| SSH_TCP_KEEPALIVE | Выставляет параметр **TCPKeepAlive** для SSH соединений, используемых скриптом. Определяет будет ли использоваться стандартные для TCP сообщения keepalive. <br> Возможные значения **yes / no**. <br> Рекомендованное значение **yes**. <br> *Реализовано в целях разработки и не рекомендуется к изменению.* |
| SSH_SERVER_ALIVE_INTERVAL | Выставляет параметр **ServerAliveInterval** для SSH соединений, используемых скриптом. Определяет промежуток времени в секундах через который утилита SSH будет отсылать keepalive сообщения по установленному соединению. <br> Рекомендованное значение **1** для быстроко обнаружения отказа сети. <br> *Реализовано в целях разработки и не рекомендуется к изменению.* |
| SSH_SERVER_ALIVE_COUNT_MAX | Выставляет параметр **ServerAliveCountMax** для SSH соединений, используемых скриптом. Определяет количество keepalive сообщений, которые утилита SSH может отправить без получения ответа перед тем, как разорвать соединение. Используется для проверки состояния СУБД и для утилиты pg_rewind. <br> Рекомендованное значение **1** для быстроко обнаружения отказа сети. <br> *Реализовано в целях разработки и не рекомендуется к изменению.* |
| PGCONNECT_TIMEOUT | Определяет время в секундах которое выделяется утилитам postgres для установления соединения с СУБД. <br> Рекомендованное значение **1** для быстроко обнаружения отказа сети СУБД. <br> *Реализовано в целях разработки и не рекомендуется к изменению.* |
| PG_STATUS_CHECK_RETRIES | Определяет количество неуспешных попыток проверки статуса инстансов СУБД в репликационной паре, после которого операция провеки статуса будет признана неуспешной. <br> Рекомендованное значение **3**. <br> *Реализовано в целях разработки и не рекомендуется к изменению.* |
| PG_STATUS_CHECK_TIMEOUT | Определяет время в секундах после после которого операции скрипта в рамках проверки статуса инстансов СУБД в репликационной паре будут признаны неуспешными. <br> Рекомендованное значение **6**. <br> *Реализовано в целях разработки и не рекомендуется к изменению.* |
| PG_REPLCATION_CHECK_RETRY_DELAY | Определяет промежуток времени в секундах через который будет проверяться использование слота репликации а также статус репликации на мастере и реплике. <br> Рекомендованное значение **5**. |
| PG_REPLICATION_CHECK_TIMEOUT | Определяет время в секундах после после которого операции проверки использования слота репликации и проверки статуса репликации на мастере и реплике признаются неуспешными. |
| PG_BASEBACKUP_RESTORE_TIMEOUT | Определяет время в секундах после которого операция снятия базовой резервной копии через pg_basebackup считается неуспешной. <br> Если установить значение параметра в **0**, механизм таймаута отключается. |
| PG_START_TIMEOUT | Определяет время в секундах после которого операция запуска инстанса СУБД считается неуспешной. <br> Если установить значение параметра в **0**, механизм таймаута отключается. |
| PG_STOP_TIMEOUT | Определяет время в секундах после которого операция остановки инстанса СУБД считается неуспешной. <br> Если установить значение параметра в **0**, механизм таймаута отключается. |
| PG_REWIND_TIMEOUT | Определяет время в секундах после которого операция отката предыдущего мастера на актуальную временную линию утилитой pg_rewind считается неуспешной. <br> Если установить значение параметра в **0**, механизм таймаута отключается. |
| STATE_VERIFICATION_TIMEOUT | Определяет время в секундах после которого процесс согласование статуса репликацонной пары признается неуспешным. Реальное время согласования состояния может достигать **3 * STATE_VERIFICATION_TIMEOUT**. <br> Если установить значение параметра в **0**, механизм таймаута отключается. <br> Рекомендованное значение **120**. <br> *Реализовано в целях разработки и не рекомендуется к изменению.* |
| STATE_VERIFICATION_RETRY_DELAY | Определяет промежуток времени в секундах через который будет проверяться статус репликацонной пары, присланный с удаленного узла. <br> Рекомендованное значение **5**. <br> *Реализовано в целях разработки и не рекомендуется к изменению.* |
| RETRY_DELAY | Определяет базовый промежуток времени в секундах через который будут повторно выполняться операции внутри скрипта. <br> Рекомендованное значение **5**. <br> *Реализовано в целях разработки и не рекомендуется к изменению.* |
| PG_BACKGROUND_CHECK_SLEEP | Определяет промежуток времени в секундах между запросами-проверками к СУБД в рамках субпроцесса мониторинга. Для мониторинга открывается активное соединение с СУБД, в которое отправляются запросы `SELECT now()`. <br> Рекомендованное значение **0.3**. <br> Меньшие значения ускоряют процесс обнаружения отказа, но увеличивают нагрузку на систему и сеть со стороны скрипта и СУБД. |
| VOTE_TIMEOUT | Определяет время в секундах после которого ожидание решения от голосующей ноды прекращается, и за результат запроса принимается отказ. <br> Если установить значение параметра в **0**, механизм таймаута отключается. <br> Рекомендованное значение **10**. |
| SSH_LOST_RECHECK_DELAY | Определяет промежуток времени в секундах между проверками сетевого соединения при его разрыве. Большие промежутки будут генерировать меньше записей об ошибке сети в логах. |
| **Поведение скрипта** | |
| LOG_LEVEL | Определяет уровень в соотвестсвии с syslog для текстового логирования: <br> &emsp; 0 - Emergency <br> &emsp; 1 - Alert <br> &emsp; 2 - Critical [FATAL] <br> &emsp; 3 - Error [ERROR] <br> &emsp; 4 - Warning [WARNING] <br> &emsp; 5 - Notice <br> &emsp; 6 - Informational [INFO] <br> &emsp; 7 - Debug [DEBUG] <br> Рекомендованный уровень логирования 5. |
| LOG_TO_JOURNAL | Определяет будет ли осуществляться логирование в системный журнал. <br> &emsp; 0 - выключает логирование <br> &emsp; 1 - включает логирование |
| JOURNAL_LOG_LEVEL | Определяет уровень в соотвестсвии с syslog для логирования в системный журнал: <br> &emsp; 0 - Emergency <br> &emsp; 1 - Alert <br> &emsp; 2 - Critical [FATAL] <br> &emsp; 3 - Error [ERROR] <br> &emsp; 4 - Warning [WARNING] <br> &emsp; 5 - Notice <br> &emsp; 6 - Informational [INFO] <br> &emsp; 7 - Debug [DEBUG] <br> Рекомендованный уровень логирования 5. |
| PG_LOG_ACHIVING | Определяет будут ли логи СУБД архивироваться и сохраняться перед деструктивными действиями. <br> &emsp; 0 - выключает сохранение <br> &emsp; 1 - включает сохранение |
| PG_ALLOW_STANDALONE_MASTER | Определяет, сможет ли мастер-инстанс СУБД при отказе реплики перейти в одиночный режим работы тем самым сохраняя доступность системы. <br> &emsp; 0 - запрещает одиночный режим работы <br> &emsp; 1 - разрешает одиночный режим работы |
| PG_USE_RECOVERY_CONF | Определяет будет ли использоваться recovery.conf вместо recovery.signal / standby.signal для управления репликами. <br> &emsp; 0 - использовать recovery.signal / standby.signal (для PostgreSQL 12+) <br> &emsp; 1 - использовать recovery.conf (для PostgreSQL 11-) |
| PG_CATCHUP_TIMEOUT | Время в минутах, которое отводится асихронной реплике на то, чтобы догнать мастер. Если реплика не догонит мастер за это время, то она будет пересоздана. Если установить значение параметра в 0, механизм таймаута отключается. |
| PG_REPLICATION_SLOT_NAME | Имя слота репликации, который будет создан для реплики. |
| SYNC_FILE_SUPPORTED | Определяет будет ли указываться имя файла при использовании команды **sync**. <br> Функаионал синхронизации кеша ФС для конкретного файла поддерживается не на всех ОС. <br> &emsp; 0 - синхронизирует кеш всей ФС <br> &emsp; 1 - синхронизирует кеш для конкретного файла <br> Реализовано в целях разработки и не рекомендуется к изменению. |
| USE_VOTER_NODE | Определяет будет ли использоваться голосующий узел для принятия кворумных решений при разрыве сетевого соединения между узлами postgres. Таким образом при разрыве сетевого соединения и отказе узла-мастера досупность сохранится путем продвижения реплики до нового мастера. <br> &emsp; 0 - не использовать голосующий узел <br> &emsp; 1 - использовать голосующий узел <br> Опция актуальна, когда местер-инстансу СУБД разрешено переходить в самостоятельный режим работы. |

### Конфигурационные параметры голосующего узла

| Параметр | Описание |
| --- | --- |
| **Фийлы и директории** | |
| Директории скрипта | |
| LOGFILE_PATH | Абсолютный путь до файла в который будут писаться логи скрипта. <br> Файл может отсутствовать, но директория файла должа существовать. |
| LOCK_FILE_DIR | Абсолютный путь до служебной директории скрипта, предназначенной для системы сигналов и блокировок. <br> Должна существовать до запуска скрипта. <br> Должнен быть идентичен на всех узлах со скриптом. |
| **Пользователи** | |
| SYSTEM_ADMIN_USER | Системный пользователь - администратор Linus. <br> Предполагается root. |
| **Информация о кластере** | |
| CLUSTER_NAME_LOCAL | Имя локального узла. Используется для записи в логи. <br> Устанавливается произвольно. |
| CLUSTER_NAME_NODE_1 | Имя кластера первого удаленного узла. Используется для записи в логи и для идентификации управляющих сигналов, предназначенных для этого узла. |
| CLUSTER_IP_NODE_1 | IPv4 адрес первого удаленного узла. |
| CLUSTER_NAME_NODE_2 | Имя кластера второго удаленного узла. Используется для записи в логи и для идентификации управляющих сигналов, предназначенных для этого узла. |
| CLUSTER_IP_NODE_2 | IPv4 адрес второго удаленного узла. |
| **Параметры ретраев и таймаутов** | |
| SSH_CONNECT_TIMEOUT | Выставляет параметр **ConnectTimeout** для SSH соединений, используемых скриптом. Определяет время в секундах которое выделяется утилите SSH для установления соединения. <br> Рекомендованное значение **1** для быстроко обнаружения отказа сети. <br> *Реализовано в целях разработки и не рекомендуется к изменению.* |
| SSH_TCP_KEEPALIVE | Выставляет параметр **TCPKeepAlive** для SSH соединений, используемых скриптом. Определяет будет ли использоваться стандартные для TCP сообщения keepalive. <br> Возможные значения **yes / no**. <br> Рекомендованное значение **yes**. <br> *Реализовано в целях разработки и не рекомендуется к изменению.* |
| SSH_SERVER_ALIVE_INTERVAL | Выставляет параметр **ServerAliveInterval** для SSH соединений, используемых скриптом. Определяет промежуток времени в секундах через который утилита SSH будет отсылать keepalive сообщения по установленному соединению. <br> Рекомендованное значение **1** для быстроко обнаружения отказа сети. <br> *Реализовано в целях разработки и не рекомендуется к изменению.* |
| SSH_SERVER_ALIVE_COUNT_MAX | Выставляет параметр **ServerAliveCountMax** для SSH соединений, используемых скриптом. Определяет количество keepalive сообщений, которые утилита SSH может отправить без получения ответа перед тем, как разорвать соединение. Используется для проверки состояния СУБД и для утилиты pg_rewind. <br> Рекомендованное значение **1** для быстроко обнаружения отказа сети. <br> *Реализовано в целях разработки и не рекомендуется к изменению.* |
| RETRY_DELAY | Определяет базовый промежуток времени в секундах через который будут повторно выполняться операции внутри скрипта. <br> Рекомендованное значение **5**. <br> *Реализовано в целях разработки и не рекомендуется к изменению.* |
| **Поведение скрипта** | |
| LOG_LEVEL | Определяет уровень в соотвестсвии с syslog для текстового логирования: <br> &emsp; 0 - Emergency <br> &emsp; 1 - Alert <br> &emsp; 2 - Critical [FATAL] <br> &emsp; 3 - Error [ERROR] <br> &emsp; 4 - Warning [WARNING] <br> &emsp; 5 - Notice <br> &emsp; 6 - Informational [INFO] <br> &emsp; 7 - Debug [DEBUG] <br> Рекомендованный уровень логирования 5. |
| LOG_TO_JOURNAL | Определяет будет ли осуществляться логирование в системный журнал. <br> &emsp; 0 - выключает логирование <br> &emsp; 1 - включает логирование |
| JOURNAL_LOG_LEVEL | Определяет уровень в соотвестсвии с syslog для логирования в системный журнал: <br> &emsp; 0 - Emergency <br> &emsp; 1 - Alert <br> &emsp; 2 - Critical [FATAL] <br> &emsp; 3 - Error [ERROR] <br> &emsp; 4 - Warning [WARNING] <br> &emsp; 5 - Notice <br> &emsp; 6 - Informational [INFO] <br> &emsp; 7 - Debug [DEBUG] <br> Рекомендованный уровень логирования 5. |
| SYNC_FILE_SUPPORTED | Определяет будет ли указываться имя файла при использовании команды **sync**. <br> Функаионал синхронизации кеша ФС для конкретного файла поддерживается не на всех ОС. <br> &emsp; 0 - синхронизирует кеш всей ФС <br> &emsp; 1 - синхронизирует кеш для конкретного файла <br> Реализовано в целях разработки и не рекомендуется к изменению. |
